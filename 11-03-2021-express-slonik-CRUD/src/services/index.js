const router = require('express').Router();

//router.use("/clients",require("./clients") )
//esto está comentado porque da error probablemente
//porque no tengo todavia nada en index de client

module.exports= router;